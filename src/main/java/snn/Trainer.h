#include <iostream>

static inline void run(void *buffer, long long offset, long long steps) {
    std::cout << "Processing in C++... " << offset << " " << %cc% << " " << steps << std::endl;
    // ...
}
